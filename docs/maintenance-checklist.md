# 保守チェックリスト

## 編集時

- `_toc.yml` と実ファイルの対応を確認した。
- 新しい依存パッケージを `requirements.txt` に追加した。
- notebook の出力に不要なビューア固有 MIME が混入していない。
- 管理者用メモを `private/` に置いた。

## 公開前

- `jb build --all .` が成功した。
- 主要ページをブラウザで確認した。
- `.ipynb` / `.md` のダウンロードを確認した。
- `git status --short` で意図した差分だけになっている。
- `_build/` が Git 管理に入っていない。

## 学期更新時

- 年度・科目情報・参考文献を確認した。
- 利用データの出典とライセンスを確認した。
- Python / Jupyter Book / 主要ライブラリの更新影響を確認した。
- `matplotlib-fontja` による日本語表示を確認した。
